import sublime
import sublime_plugin


class DragSelectJapaneseCommand(sublime_plugin.TextCommand):
  def run(self, edit):
    expand_by_class(point, classes, <separators>) 
    print(self.view.substr(self.view.sel()[0]))
    # self.view.insert(edit, 0, self.view.sel())
